tree("Root")
    fallback 
        tree("CheckForEnemies")
        tree("Hunt")
	    
tree("CheckForEnemies")
    sequence
        CheckIfEnemyClose()
        FleeFromEnemy()
            
tree("Hunt")
    sequence
        CheckIfFoodClose()
        fallback
            AttackTarget()
            EatTarget()