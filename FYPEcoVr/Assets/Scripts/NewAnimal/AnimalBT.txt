tree("Root")
    fallback 
        tree("CheckForEnemies")
        tree("Hunt")
        tree("Wander")
	    
tree("CheckForEnemies")
    sequence
        CheckIfEnemyClose()
        FleeFromEnemy()
            
tree("Hunt")
    sequence
        CheckIfFoodClose()
        fallback
            SeekTarget()
            sequence
                ArriveTarget()
                AttackTarget()
                EatTarget()
           
tree("Wander")
    sequence
        fallback
            CheckWanderTarget()
            GetWanderTarget()
        fallback
            SeekTarget()
            GetWanderTarget()
        
 
        