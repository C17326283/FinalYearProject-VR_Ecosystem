tree("Root")
    fallback 
        tree("CheckForEnemies")
        tree("Hunt")
        tree("Wander")
	    
tree("CheckForEnemies")
    sequence
        CheckIfEnemyClose()
        FleeFromEnemy()
            
tree("Hunt")
    sequence
        fallback
            sequence
                IsHungry()
                CheckIfRemembersResource("Food")
            sequence
                IsThirsty()
                CheckIfRemembersResource("Water")
        fallback
            sequence
                SeekTarget()
                CheckGettingCloser()
            sequence
                ArriveTarget()
                fallback
                    AttackTarget()
                    ConsumeTarget()
           
tree("Wander")
    sequence
        fallback
            CheckWanderTarget()
            GetWanderTarget()
        fallback
            sequence
                SeekTarget()
                CheckGettingCloser()
            GetWanderTarget()
        
 
        