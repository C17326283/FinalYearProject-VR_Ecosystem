tree("Root")
    fallback 
        tree("CheckForEnemies")
        tree("Hunt")
        tree("Wander")
	    
tree("CheckForEnemies")
    sequence
        CheckIfEnemyClose()
        FleeFromPredator()
            
tree("Hunt")
    sequence
        fallback
            sequence
                IsHungry()
                fallback
                    sequence
                        GetAnimalTarget()
                        ChasePrey()
                    sequence
                        CheckIfRemembersResource("Food")
                        SeekTarget()
            sequence
                IsThirsty()
                CheckIfRemembersResource("Water")
                SeekTarget()
        fallback
            sequence
                CheckGettingCloser()
            sequence
                ArriveTarget()
                fallback
                    sequence
                        AttackTarget()
                    ConsumeTarget()
           
tree("Wander")
    sequence
        fallback
            CheckWanderTarget()
            GetWanderTarget()
        fallback
            sequence
                SeekTarget()
                CheckGettingCloser()
            GetWanderTarget()
        
 
        